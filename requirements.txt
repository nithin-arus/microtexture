# Core dependencies for data collection and feature extraction
# NOTE: For reproducibility, generate requirements-frozen.txt using: pip freeze > requirements-frozen.txt
# This file uses >= for flexibility during development. For production/reproduction, use requirements-frozen.txt

opencv-python>=4.8.0
numpy>=1.24.0
pandas>=2.0.0
scikit-image>=0.20.0
scipy>=1.10.0
matplotlib>=3.7.0
picamera2>=0.3.12
tqdm>=4.65.0
PyWavelets>=1.4.0
mahotas>=1.4.0

# Research framework dependencies
scikit-learn>=1.0.0
seaborn>=0.12.0
umap-learn>=0.5.0
joblib>=1.2.0

# Enhanced ML and deep learning dependencies
xgboost>=1.7.0               # For XGBoost classifier
lightgbm>=3.3.0              # For LightGBM classifier  
tensorflow>=2.10.0           # For deep learning CNN feature extraction
keras>=2.10.0                # For neural network models

# Advanced feature selection and optimization
optuna>=3.0.0                # For hyperparameter optimization
imbalanced-learn>=0.9.0      # For SMOTE and feature augmentation

# Advanced visualization
plotly>=5.0.0                # For interactive visualizations

# Optional explainability dependencies (install as needed)
# shap>=0.41.0               # For SHAP explainability
# lime>=0.2.0                # For LIME explainability

# To generate frozen requirements for reproducibility:
# 1. Create a fresh virtual environment
# 2. Install all dependencies: pip install -r requirements.txt
# 3. Generate frozen file: pip freeze > requirements-frozen.txt
# 4. Use requirements-frozen.txt for exact reproduction 